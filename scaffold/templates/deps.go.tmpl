// Package deps defines the agent's typed dependency graph.
package deps

// Deps contains the services and SDK handles shared by handlers and tools.
// Add required constructor parameters and fields together so missing wiring
// fails while the composition root is compiled.
type Deps struct{}

// New constructs the complete dependency graph.
func New() *Deps {
	return &Deps{}
}
