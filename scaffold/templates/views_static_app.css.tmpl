/* Placeholder — overwritten by the Docker build's
 *   .airlock/toolchain/bin/tailwindcss -i styles/app.css -o views/static/app.css --minify
 * step. This file exists only so views/assets.go's //go:embed compiles
 * on a fresh checkout before the first build has run. Don't edit; the
 * source is styles/app.css.
 */
